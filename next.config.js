/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
            "localhost",
            "digitalhippo-production-6a3a.up.railway.app"
        ],
	},
};

module.exports = nextConfig;